extends layout

block content
  .grid.example
    .item.six
      .item.twelve(style="background-image:url(/processing-experiments/#{directory}/#{image})")
      a(href="/zip/#{directory}") Download Zip
    .item.six
      pre
        code(class="java")!= codez
